import { Flex, styled } from '$/jsx'

const Card = styled(Flex, {
  base: {
    flexDirection: 'column',
    padding: 32,
    borderRadius: 8,
    backgroundColor: 'gray100'
  }
})

export default Card
